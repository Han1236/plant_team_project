# FastAPI 앱을 위한 Dockerfile
FROM python:3.9-slim

# 작업 디렉토리 설정
WORKDIR /app

# 의존성 파일 복사
COPY requirements.txt .

# 의존성 설치
RUN pip install --no-cache-dir -r requirements.txt

# FastAPI 앱 파일 복사
COPY . .

# FastAPI 실행 (uvicorn을 사용하여 앱 실행)
CMD ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
